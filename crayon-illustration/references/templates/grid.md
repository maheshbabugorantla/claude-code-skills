# Template: `grid`

**Layout:** Cards in a grid (typically 4x2) with icons and descriptions.

## Prompt Template

```
Create a hand-drawn crayon-style illustration on a white background showing
{{N}} {{TOPIC}} cards in a {{COLS}}x{{ROWS}} grid.

Title at top: "{{GRID_TITLE}}"

Row 1 ({{COLS}} cards):

Card 1 (pastel yellow, sketchy border):
- Crayon drawing of {{icon description}}
- Title: "{{card title}}"
- Body: "{{card description}}"

Card 2 (pastel blue, sketchy border):
- Crayon drawing of {{icon description}}
- Title: "{{card title}}"
- Body: "{{card description}}"

{{...more cards...}}

Each card slightly rotated (hand-placed feel). All text handwritten style.
{{STYLE BLOCK}}
```
