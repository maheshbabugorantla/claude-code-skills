# Template: `mapping`

**Layout:** Two columns with dotted hand-drawn lines connecting matching concepts.

## Prompt Template

```
Create a hand-drawn crayon-style educational illustration on a white background
showing how {{LEFT_CONCEPT}} concepts map to {{RIGHT_CONCEPT}}.

Layout: Two columns with dotted hand-drawn lines connecting matching concepts.

Left column header (dark teal card): "{{LEFT_HEADER}}"
Right column header (dark green card): "{{RIGHT_HEADER}}"

Row 1:
- Left card (pastel blue): "{{left 1}}" with small drawing of {{icon}}
- Right card (pastel green): "{{right 1}}" with small drawing of {{icon}}
- Dotted line connecting them

Row 2:
- Left card (pastel blue): "{{left 2}}" with small drawing of {{icon}}
- Right card (pastel green): "{{right 2}}" with small drawing of {{icon}}
- Dotted line connecting them

{{...more rows as needed...}}

{{STYLE BLOCK}}
```
